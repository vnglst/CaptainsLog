# Testing Plan: End-to-End Coverage Without Xcode

**Status:** Deterministic plan is implemented; 184 full-suite checks and 132 lightweight unit checks pass. Hardware capture and the standalone installed-model smoke remain opt-in integration gates. Remaining OS-owned/native gaps are listed below.  
**Baseline reviewed:** 2026-09-26

## Goal

Cover every user-visible app workflow through a combination of deterministic command-line tests, full CLI workflows, stage evaluations with human semantic review, and targeted native UI checks. Builds and deterministic tests must use Swift Package Manager and Apple Command Line Tools; no Xcode IDE, XCTest, or Swift Testing dependency is allowed. Model and recording checks remain opt-in because they need downloaded models or real hardware.

The test runtime must be isolated. Set `CAPTAINS_LOG_CONFIG_PATH` to a temporary config before launching the CLI or app, set its `dataDir` to a temporary directory, and seed that directory only from repository fixtures under `eval/`. Never use `processed/`, Obsidian, a user's CaptainsLog directories, or the default `DemoMode` data (which copies `demo/`) as a test source. `DesignFixtures.swift` contains hard-coded presentation examples; do not count those as repository test fixtures under the privacy rule. Add explicit cases under `eval/ui/`, load only those files into the isolated runtime, and use state injection only to select presentation state.

## Existing foundation and gaps

`swift run run-tests` executes a framework-free test executable in `Sources/run-tests/main.swift`. GitHub Actions uses `swift run run-tests --unit`, which omits the separate workflow, search-index, coordinator, and pipeline-orchestration groups; running without `--unit` executes the full suite. Unit tests include config persistence/migration, malformed-config recovery, directory precedence, prompt construction, filename normalization, local GGUF selection, categorization parsing, pipeline stage selection/recovery, search manager transitions, Markdown parsing, the pure UI policy/state for onboarding, entry navigation, row actions, the recording dock and queue strip, plus recorder lifecycle through injected fake engine/writer operations (device selection/fallback, tap install, RMS levels, pause gating, write/start failures and release). They also cover model readiness/download progress, directory watcher lifecycle, and AppState transitions. Workflow tests cover Logs loading/deletion, resume/queue/pause/retry, row errors, missing-audio safety, onboarding persistence, fresh audio-copy runs, audio-import safety, each pipeline resume stage, settings reload, folder-picker outcomes, and context reload. Search tests cover empty queries, frontmatter indexing, ranking, manager cancellation/invalidation/reuse/retry. CLI fake-operation checks cover transcribe, cleanup, enrich, filename and categorize success/failure; pipeline tests cover fresh runs, stage failures, missing audio and all resume paths. Latest full-suite run: **184/184 passed**; latest lightweight suite: **132/132 passed**. `scripts/test-coverage.sh` checks command help, validation, temp-config persistence, six `cl list` stages and prompt rendering. Latest coverage: **31.58%** all-source line coverage (37.86% functions; 41.82% regions) and **82.16%** deterministic-production line coverage, 2.16 percentage points above the enforced 80% floor. Scope and exclusions are documented in ADR-007.

Most workflow tests assert user-visible state plus resulting files: CLI fake operations exercise stage outputs and failures, AppState exposes row-level errors and retries, and pipeline resume tests inspect preserved or regenerated artifacts. Remaining gaps include the raw AVFoundation adapter boundary, some OS-owned panel outcomes, and further extraction of UI decisions where it creates a useful deterministic seam. `scripts/test-recorder-hardware.sh` and `scripts/test-model-smoke.sh` define opt-in gates; neither was run as part of this deterministic verification. Inference quality remains a separate human-reviewed eval gate; the current transcription report records a quality failure rather than a pass.

## Coverage matrix

| User-visible surface / workflow | Deterministic CLI or framework-free tests | Eval semantic review | Native app UI check | Current gap / acceptance condition |
|---|---|---|---|---|
| First launch and onboarding | `Onboarding: completing first run dismisses setup and persists the launch choice` asserts persistence with UserDefaults restored after test | None | Harness `onboarding` selector; native verified | Folder validation/select/cancel OS flow remains unverified natively. |
| Logs list, empty state, grouping, row status | `AppState: loads completed and pending eval entries for the Logs screen`; `AppState: LogEntry parses completed-entry frontmatter`; native fixture rendering recorded in `TESTING-UI-REPORT.md` | None | Empty/populated/processing/paused/failed states passed; details in `TESTING-UI-REPORT.md` | No layout geometry or alternate-window-size check; not all row menu actions are exercised. |
| Entry detail (read only) | `AppState: LogEntry parses completed-entry frontmatter`; `AppState: LogEntry preserves Markdown without frontmatter`; empty body and metadata checked in eval-backed Logs workflow | Enrichment fixture provides the rendered content | Native detail open/back pass | No edit/save control exists in the production detail view. Finder reveal was not activated. |
| Delete flow | Completed eval log deletion removes all artifacts; active processing blocks deletion; failed Trash move leaves the entry visible | None | Confirm/cancel and verify disappearance against disposable eval copies; pass | The native Trash-provider error itself has no dedicated error alert; current behavior keeps the remaining entry listed. |
| Recording dock and device selector | `RecordingState: fake recorder supports pause, resume, and stop, receives the selected device UID and isolated audio path, and failure clears the meter and duration timer`; normal return and thrown cancellation both suppress completion/error callbacks and stop the timer; background refresh preserves a connected device selection and falls back to the default when it disconnects; injected coordinator/recorder paths | Transcription eval from synthetic audio after capture path is separately validated | Idle, recording, paused, and disabled-no-device states rendered; hardware actions skipped | AVFoundation device capture and actual selection still require manual hardware check. Never record during automated suite. |
| Processing queue / resume / retry | `AppState: resumes a pending log through injected pipeline operations`; `ProcessingCoordinator: drains queued work through injected pipeline`; `ProcessingCoordinator: batch resume continues after one entry fails`; `ProcessingCoordinator: retry clears a prior failure after success`; `ProcessingCoordinator: pausing an active queue stays paused after cancellation`; `Pipeline: fake cleanup failure stops later stages, preserves the transcript, and remains resumable at cleaning`; `Resume CLI: cleanup cancellation preserves the transcript and remains retryable`; `Resume CLI: malformed category manifest reruns categorization before later stages`; `Resume CLI: failed categorization leaves a malformed manifest retryable`; `Resume CLI: rejects unsafe filename values before writing in the rename stage`; `Pipeline: unsafe stored slugs cannot expose or delete files outside the data folder`; `Pipeline: fake fresh run copies eval audio and writes all stage artifacts`; importing an audio file already in the destination preserves it, and missing input preserves an existing destination; all six resume starts use fake operations; an explicit later resume stage falls back to cleaning if its cleaned artifact is missing | Full pipeline on one designated eval audio fixture; review every stage artifact | Active, paused and failed pass; retry/resume actions skipped in UI | Native retry/resume action remains unverified; never activate actual model-backed controls in fixture UI. |
| Semantic search | `Semantic search: blank and zero-limit queries return without inference`; `Semantic search: frontmatter remains searchable when the body is empty`; `Semantic search: indexes, ranks, updates, and removes completed entries`; `Hybrid search: keyword passage is centered and highlighted`; search manager state tests | `eval/search/queries.json` and synthetic notes; review relevance, omissions and false positives | Results/select, no-results, error/safe retry, preparing, indexing, searching passed | Live embedding retrieval remains opt-in; review the eval report for semantic relevance. |
| Settings: data folder | CLI config persistence checks; fake picker checks the configured initial folder, selection, cancel, demo-mode guard, missing-folder fallback, and persistence | None | Open/cancel native panel in isolated runtime and verify it starts in configured temp path | Finder reveal remains unverified. |
| Settings: models | Test model status ready/downloading/error, sequential Whisper/Qwen progress reporting with gated fake downloads, and AppState presentation mapping | Transcription and text-model eval outputs | Ready/downloading/error status displays passed; no model control is exposed in Settings | UI exposes status only. Real downloads/model loading are opt-in, sequential. |
| Settings: context and corrections | Config round-trip, manager reload, data-folder switching without leaking prior context, and saved context/corrections inclusion in a cleanup prompt | Cleanup, filename, enrichment semantic reviews | Context/corrections edits wrote to temp files | Native relaunch persistence and full UI-to-prompt integration remain untested. |
| Settings: storage and audio input | Test config dataDir and device selection/refresh through injected boundaries | None | Configured folder picker open/cancel and Refresh devices display checked; Reveal in Finder not activated | Finder integration remains manual; do not start microphone capture. |
| Settings: about and notices | Test version formatting and bundled notice resource availability | None | Third-party notices open/render/Done checked | External source links were not opened. |
| CLI recording, transcribe, cleanup, categorize, filename, enrich | `scripts/test-coverage.sh` asserts help for every leaf command, required-argument rejection, missing-file rejection before model loading, and non-positive duration rejection before recording; renders cleanup/filename/categorize/enrich prompts, including each categorization fixture; fake transcribe CLI asserts options/default output and preserves old output on inference failure, plus surfaces output-write failure; fake cleanup and enrich CLI success/failure asserts output preservation and omitted-output-path behavior in an isolated working directory; fake filename CLI asserts date/prompt handoff and slug; fake categorize CLI asserts manifest and diagnostics; pipeline CLI tests exercise fresh runs, copy errors, stage resumes, and batch resume | Transcription/cleanup/filename/enrichment suites generated and reviewed; standalone categorization suite checks four manifest outputs against expected labels | N/A | Successful fake-backed transcription, cleanup, enrichment, filename generation, and categorization are covered; missing-input and injected inference failures are tested, including transcription, cleanup, and enrichment output-write plus categorization manifest-write failures; filesystem failure paths in other commands remain incomplete. Actual recording still needs hardware; inference-backed transcription remains opt-in. |
| CLI pipeline, resume, list, search, config | `scripts/test-coverage.sh` runs `cl list` against synthetic artifacts at all six pipeline stages; it also covers config persistence, empty resume and invalid resume stages. Temp fixture `cl pipeline` completed all stages; resume immutability and search indexing/readback are asserted in `scripts/run-evals.sh --pipeline` | Semantic stage changes and model variance reviewed in `TESTING-EVAL-REPORT.md` | N/A | Fresh-pipeline failure injection verifies prior-artifact preservation and retry-stage detection at every stage; malformed directory artifacts fall back to the earliest incomplete stage. Search query relevance breadth remains a gap. |

## Evaluation suite and review

Keep stage-specific evaluation instructions in `skills/<stage>-eval/SKILL.md` as the source of command details. Run one inference task at a time, sequentially, because WhisperKit and llama.cpp share on-device GPU memory. Reuse one loaded Qwen model within each pipeline run. For every run, save generated output and a report beneath the suite's ignored `generated/` and `reports/` directories. This pass generated and semantically reviewed transcription, cleanup, filename and enrichment results; the semantic summary is [`TESTING-EVAL-REPORT.md`](./TESTING-EVAL-REPORT.md). Transcription failed quality review because proper names/topic phrases were changed or omitted. Pipeline artifact assertions and search readback passed; they do not override the semantic quality failure.

1. Transcription: the sequential run reviewed all four synthetic audio fixtures under `eval/transcribe/audio/` against `eval/transcribe/expected/`. Reports found dropped/changed proper names and phrases, a topic-changing “science project” recognition, and an added `[BLANK_AUDIO]` marker. Quality gate: failed; see `TESTING-EVAL-REPORT.md`.
2. Cleanup: all three inputs were reviewed against `eval/cleanup/expected/`; see `TESTING-EVAL-REPORT.md` for preserved facts and omissions.
3. Categorization: `eval/categorize/` has four synthetic cases covering all three labels and a mixed memo; `bash scripts/run-evals.sh --categorize` runs them sequentially and validates JSON structure, source stem, and expected label. Human semantic review is recorded in `TESTING-EVAL-REPORT.md`.
4. Filename: all eight cases were generated and structurally checked; semantic changes including lost contrast/context are documented in `TESTING-EVAL-REPORT.md`.
5. Enrichment: all four cases were reviewed; saved outputs passed YAML/frontmatter validation after a sequential rerun of one initially empty artifact. See `TESTING-EVAL-REPORT.md`.
6. Search: use every query in `eval/search/queries.json` against a clean index of the synthetic notes in `eval/search/`; `eval/search/README.md` documents these fixtures. Review ranking, expected hit visibility, false positives, and useful excerpts.
7. End-to-end pipeline: the `2025-01-14 side project` synthetic audio fixture completed every stage in isolated temp data; resume was idempotent and search readback found the final entry. Semantic issues and a variable derived recording time are documented in `TESTING-EVAL-REPORT.md`.

Do not make model evaluations parallel. Reports need examples of semantic changes and must record model, prompt/config, fixture, command, and date so results are reproducible. Human review remains the final quality judgment.

## Execution sequence

1. **Inventory and baseline:** map every registered CLI command and every visible control in the production `FieldNotesContentView`; capture existing deterministic tests and current coverage JSON when the implementer runs the baseline. Keep the legacy `ContentView` separate until dead-code evidence is reviewed.
2. **Deterministic behavior:** add seams and tests for onboarding, read-only entry list/detail/delete decisions, recording state, coordinator queue/retry, config and settings logic, and pipeline recovery. Use temporary directories and fakes. Each failure test must check user-visible error/state and resulting files.
3. **CLI E2E:** parser/help and fake-backed command workflows cover registered leaf commands, isolated config persistence, command validation, stage outputs, fresh pipeline failures, and resume from every stage. `scripts/test-coverage.sh` runs CLI smoke checks with temporary config and synthetic filesystem fixtures; GitHub Actions stays on the separate lightweight unit subset.
4. **Evaluation gate:** `scripts/run-evals.sh --pipeline` and `--suites` ran sequentially; saved artifacts passed structural validation and were reviewed in `TESTING-EVAL-REPORT.md`. Transcription quality failed semantic review despite pipeline assertions passing; fix prompts/model quality and repeat the same fixture review before claiming eval quality pass.
5. **Native UI gate:** `scripts/test-ui.sh` packages a SwiftPM debug build and seeds a temporary runtime only from repository `eval/` files. `CAPTAINSLOG_UI_FIXTURE` selects display states; it skips startup watcher/device discovery/model download/bootstrap, while controls remain real. Record each state and interaction in `docs/TESTING-UI-REPORT.md`. Do not activate inference- or recording-triggering controls. Keep hardware-only recording and system folder/Finder/link panels as explicit manual checks. A fixture render does not prove backend behavior.
6. **Coverage and maintenance:** run `swift run run-tests`, `bash scripts/test-coverage.sh`, `swift build`, and the affected CLI workflows with repository fixtures. After feature/refactor/dependency changes, follow AGENTS: run `swift build`, `swift run run-tests`, and a full sequential pipeline fixture; evaluation suites are a quality gate. Keep the 80% deterministic-production line floor stable as behavior coverage grows, and report full-source coverage and SwiftUI/native integration files separately.

## Completion criteria

- Every visible production control and state has either an automated behavior assertion or a named native UI check; each OS/hardware-only gap is explicit.
- Every CLI subcommand has help/validation coverage and a successful path or a justified native/model-only limitation.
- Deterministic tests run with Swift and Command Line Tools without Xcode IDE, network, models, or audio capture.
- Stage evals include semantic review, and one synthetic audio fixture runs through all pipeline stages in sequence.
- All test runtimes use temporary configuration and eval-only inputs; test outputs remain outside repository user data.
- `docs/DEAD-CODE-REPORT.md` tracks evidence-based cleanup candidates separately from confirmed unreachable code.
